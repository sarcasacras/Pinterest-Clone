# Frontend Environment Variables
# Copy this file to .env and fill in the actual values
# Note: All Vite environment variables must be prefixed with VITE_

# Backend API Configuration
# URL where your backend server is running
VITE_API_BASE_URL=http://localhost:3000

# ImageKit Configuration
# Same endpoint as backend, but needed for frontend image uploads
VITE_IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your_imagekit_id/

# Development Notes:
# - Frontend env vars are exposed to the browser, so never put secrets here
# - Use different API URLs for different environments (dev/staging/production)
# - ImageKit URL endpoint should match the backend configuration
# - In production, change VITE_API_BASE_URL to your production backend URL

# Production Example:
# VITE_API_BASE_URL=https://api.yourpinterestclone.com
# VITE_IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/production-id/